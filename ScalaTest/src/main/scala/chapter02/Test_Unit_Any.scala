package chapter02

object Test_Unit_Any {
  /**
   * Unit: 表示无值，和其他语言中void等同，用作不返回任何结果的方法的结果 Unit只有一个实例
   * Null： null，Null 类型只有一个实例值 null
   * Nothing：Nothing类型在scala的类层级中最低，它是任何其他类型的子类型，当一个函数，我们确定没有正常的返回值，可以使用；引用类型可以赋值为null
   * nothing来指定返回类型，这样有一个好处，我们可以把返回的（异常值）赋值给其他函数或者变量
   * @param args
   */
  def main(args: Array[String]): Unit = {

  }
}
